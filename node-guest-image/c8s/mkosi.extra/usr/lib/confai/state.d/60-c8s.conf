# rke2 writes its kubeconfig (rke2.yaml) and node/password here at runtime,
# and the pre-start units drop fragments into config.yaml.d (see the header
# of etc/rancher/rke2/config.yaml). The overlay also makes the baked
# config.yaml / psa-config.yaml / audit-policy.yaml root-shadowable, which
# changes nothing: a fragment in config.yaml.d already overrides them.
etc/rancher
