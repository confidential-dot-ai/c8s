# Propagate CDH GHCR auth to all systemd children (including kata-agent
# and the confidential-data-hub subprocess it spawns).
[Manager]
DefaultEnvironment="CDH_DEFAULT_IMAGE_AUTHENTICATED_REGISTRY_CREDENTIALS=file:///run/image-security/auth.json"
