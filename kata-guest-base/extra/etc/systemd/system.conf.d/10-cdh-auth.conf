# Propagate the CDH registry-auth path to all systemd children (including
# kata-agent and the confidential-data-hub subprocess it spawns).
[Manager]
DefaultEnvironment="CDH_DEFAULT_IMAGE_AUTHENTICATED_REGISTRY_CREDENTIALS=file:///run/image-security/auth.json"
