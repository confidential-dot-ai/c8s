# Build output
build/


# Stray root-level binaries from `go build ./cmd/<name>` without -o build/
# (Makefile targets land artifacts in build/; anything matching a cmd/
# subdir at repo root is an accidental compile output).
/c8s
/get-cert
/node-container-allowlist
/nri-image-policy
/ratls-mesh

# Go workspace sum
go.work.sum

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Test
*.test
*.out
coverage.txt
# Mock binaries from `go build .` in the test/mock-* modules.
/test/mock-attestation/mock-attestation
/test/mock-cds/mock-cds

# Secrets
*.pem
*.key


# confos fragment-build output; the committed drift baseline stays separate.
